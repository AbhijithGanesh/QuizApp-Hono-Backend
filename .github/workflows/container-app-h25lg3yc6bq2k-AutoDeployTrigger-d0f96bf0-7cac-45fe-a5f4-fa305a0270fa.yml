name: Trigger auto deployment for container-app-h25lg3yc6bq2k

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '//**'
    - '.github/workflows/container-app-h25lg3yc6bq2k-AutoDeployTrigger-d0f96bf0-7cac-45fe-a5f4-fa305a0270fa.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPH25LG3YC6BQ2K_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPH25LG3YC6BQ2K_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPH25LG3YC6BQ2K_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPH25LG3YC6BQ2K_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPH25LG3YC6BQ2K_REGISTRY_PASSWORD }}
          containerAppName: container-app-h25lg3yc6bq2k
          resourceGroup: quizapphonobackend910b
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


