name: Trigger auto deployment for container-app-we6wg2cp3ahjw

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '//**'
    - '.github/workflows/container-app-we6wg2cp3ahjw-AutoDeployTrigger-98bea432-74d8-477d-bcab-342353cc9be2.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPWE6WG2CP3AHJW_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPWE6WG2CP3AHJW_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPWE6WG2CP3AHJW_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPWE6WG2CP3AHJW_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPWE6WG2CP3AHJW_REGISTRY_PASSWORD }}
          containerAppName: container-app-we6wg2cp3ahjw
          resourceGroup: quizapphonobackenda53f
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


